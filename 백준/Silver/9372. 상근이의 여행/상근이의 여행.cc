#include<iostream>
using namespace std;

int t;
int main() {
    cin >> t;
    while(t--) {
        int n,m;
        cin >> n >> m;
        for(int i=0; i<m; ++i) {
            int a, b;
            cin >> a >> b;
        }
        cout << n-1 << "\n";
    }
}